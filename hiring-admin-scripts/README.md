

## DOWNLOAD RESUMES
- run persistent port
```bash
start chrome.exe --remote-debugging-port=9222 --user-data-dir="C:\selenum\ChromeProfile"
```
- in the opened port, log into your cwl so you have access to ubc qualtrics
- pip install necessary libraries in `download_resumes.py`
- open the file `download_resumes.py` and edit the url variable 
- run `download_resumes.py`

